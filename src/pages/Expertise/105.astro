---
import Layout from '../../layouts/Layout.astro';

/* =========================================================
   EDIT ONLY THIS LIST — title + caption for each image
   Keys must match EXACT filenames in:
     /src/assets/expertise files/105/
   ========================================================= */
const texts: Record<string, { title: string; caption: string }> = {

  // ===== IMAGE: IMG_3601.JPG =====
  "IMG_3601.JPG": {
    title:   "Placeholder title for IMG_3601",
    caption: "Placeholder caption describing what is happening in this image.",
  },

  // ===== IMAGE: IMG_4449.JPG =====
  "IMG_4449.JPG": {
    title:   "Placeholder title for IMG_4449",
    caption: "Placeholder caption describing what is happening in this image.",
  },

  // ===== IMAGE: IMG_5774.PNG =====
  "IMG_5774.PNG": {
    title:   "Placeholder title for IMG_5774",
    caption: "Placeholder caption describing what is happening in this image.",
  },
};
/* ===== END OF EDITABLE SECTION ===== */

function byFilename(pathA: string, pathB: string) {
  const name = (p: string) => p.split('/').pop() ?? p;
  return name(pathA).localeCompare(name(pathB), undefined, { numeric: true, sensitivity: 'base' });
}

const raw = import.meta.glob<{ default: { src: string } }>(
  '../../assets/expertise files/105/*.{jpg,jpeg,png,webp,JPG,JPEG,PNG,WEBP}',
  { eager: true }
);

const slides = Object.entries(raw)
  .sort(([a], [b]) => byFilename(a, b))
  .map(([path, mod]) => {
    const filename = path.split('/').pop() ?? path;
    const t = texts[filename] ?? { title: "", caption: "" };
    return { src: mod.default.src, filename, title: t.title, caption: t.caption };
  });

const captionsJSON = JSON.stringify(slides.map(s => ({ title: s.title, caption: s.caption })));
---
<Layout title="Water Heater Installation & Water Treatment Minneapolis | HandyMSP">
    <div class="flex flex-col gap-12 sm:gap-16">
        <!-- HERO -->
        <section class="rounded-xl bg-gradient-to-br from-slate-800 to-slate-900 p-8 sm:p-12 text-center border border-slate-700">
            <p class="text-blue-400 font-semibold text-sm uppercase tracking-widest mb-3">
                Minneapolis &amp; Twin Cities Suburbs
            </p>
            <h1 class="text-3xl sm:text-4xl font-bold text-white mb-4">
                Hot Water When You Need It, Clean Water Always
            </h1>
            <p class="text-lg text-slate-300 mb-8 max-w-2xl mx-auto">
                Tank, tankless, heat pump and solar water heaters — plus whole-home water treatment, installed by licensed pros.
            </p>
            <div class="flex flex-col sm:flex-row gap-4 justify-center">
                <a href="#quote"
                   class="inline-block bg-blue-600 hover:bg-blue-500 text-white font-bold px-8 py-4 rounded-lg text-lg transition">
                    Get Your Free Water Heaters & Treatment Estimate
                </a>
                <a href="tel:7609721009"
                   class="inline-block bg-white hover:bg-slate-100 text-slate-900 font-bold px-8 py-4 rounded-lg text-lg transition">
                    &#128222; 760-972-1009
                </a>
            </div>
        </section>

        <!-- PAIN POINTS -->
        <section>
            <h2 class="text-2xl font-bold text-white mb-8 text-center">Sound Familiar?</h2>
            <div class="grid grid-cols-1 sm:grid-cols-3 gap-6">
                <div class="bg-slate-800 rounded-xl p-6 border border-slate-700">
                    <h3 class="font-bold text-white text-lg mb-2">Cold showers and a leaking tank?</h3>
                    <p class="text-slate-300 text-sm leading-relaxed">Most water heaters last 8–12 years. We'll let you know honestly if repair makes sense — or get a new unit installed same-day in many cases.</p>
                </div>
                <div class="bg-slate-800 rounded-xl p-6 border border-slate-700">
                    <h3 class="font-bold text-white text-lg mb-2">Running out of hot water?</h3>
                    <p class="text-slate-300 text-sm leading-relaxed">Tankless and heat pump water heaters give endless hot water and lower bills. We size the right unit for your home and family.</p>
                </div>
                <div class="bg-slate-800 rounded-xl p-6 border border-slate-700">
                    <h3 class="font-bold text-white text-lg mb-2">Hard water wrecking fixtures & appliances?</h3>
                    <p class="text-slate-300 text-sm leading-relaxed">Twin Cities water is hard. Softeners, filters, and full treatment systems extend the life of your plumbing and skin.</p>
                </div>
            </div>
        </section>

        <!-- WHY HANDYMSP -->
        <section class="bg-slate-800 rounded-xl p-8 border border-slate-700">
            <h2 class="text-2xl font-bold text-white mb-6 text-center">
                Why Twin Cities Homeowners Choose HandyMSP
            </h2>
            <div class="grid grid-cols-1 sm:grid-cols-2 gap-4">
                <div class="flex items-start gap-3">
                    <span class="text-green-400 font-bold text-lg flex-shrink-0">&#10003;</span>
                    <p class="text-slate-300 text-sm leading-relaxed">Serving Edina, Eden Prairie, Minnetonka, Plymouth, Wayzata, Hopkins, St. Louis Park, Bloomington, Burnsville, Eagan, Woodbury, Maple Grove, and surrounding Twin Cities suburbs</p>
                </div>
                <div class="flex items-start gap-3">
                    <span class="text-green-400 font-bold text-lg flex-shrink-0">&#10003;</span>
                    <p class="text-slate-300 text-sm leading-relaxed">Licensed, insured &amp; background-checked technicians</p>
                </div>
                <div class="flex items-start gap-3">
                    <span class="text-green-400 font-bold text-lg flex-shrink-0">&#10003;</span>
                    <p class="text-slate-300 text-sm leading-relaxed">Same-week appointments available</p>
                </div>
                <div class="flex items-start gap-3">
                    <span class="text-green-400 font-bold text-lg flex-shrink-0">&#10003;</span>
                    <p class="text-slate-300 text-sm leading-relaxed">100% satisfaction guarantee — or we make it right</p>
                </div>
            </div>
        </section>

        <!-- WHAT WE HANDLE -->
        <section>
            <h2 class="text-2xl font-bold text-white mb-6">What We Handle</h2>
            <div class="grid grid-cols-1 sm:grid-cols-2 gap-3">
                <div class="flex items-start gap-3 bg-slate-800 rounded-lg p-3 border border-slate-700">
                    <span class="text-blue-400 font-bold flex-shrink-0">&#10003;</span>
                    <span class="text-slate-300 text-sm">Tank water heater installation & repair</span>
                </div>
                <div class="flex items-start gap-3 bg-slate-800 rounded-lg p-3 border border-slate-700">
                    <span class="text-blue-400 font-bold flex-shrink-0">&#10003;</span>
                    <span class="text-slate-300 text-sm">Tankless water heaters</span>
                </div>
                <div class="flex items-start gap-3 bg-slate-800 rounded-lg p-3 border border-slate-700">
                    <span class="text-blue-400 font-bold flex-shrink-0">&#10003;</span>
                    <span class="text-slate-300 text-sm">Heat pump water heaters</span>
                </div>
                <div class="flex items-start gap-3 bg-slate-800 rounded-lg p-3 border border-slate-700">
                    <span class="text-blue-400 font-bold flex-shrink-0">&#10003;</span>
                    <span class="text-slate-300 text-sm">Solar water heaters</span>
                </div>
                <div class="flex items-start gap-3 bg-slate-800 rounded-lg p-3 border border-slate-700">
                    <span class="text-blue-400 font-bold flex-shrink-0">&#10003;</span>
                    <span class="text-slate-300 text-sm">Water softener & filtration systems</span>
                </div>
                <div class="flex items-start gap-3 bg-slate-800 rounded-lg p-3 border border-slate-700">
                    <span class="text-blue-400 font-bold flex-shrink-0">&#10003;</span>
                    <span class="text-slate-300 text-sm">Whole-home water treatment</span>
                </div>
            </div>
        </section>

        <!-- ACCORDION: SERVICE CATEGORIES -->
        <section>
            <h2 class="text-2xl font-bold text-white mb-2">All Water Heaters & Treatment Services</h2>
            <p class="text-slate-400 mb-6">Click any category below to expand.</p>
            <div class="flex flex-col gap-3">
                <details class="group bg-slate-800 rounded-xl border border-slate-700 overflow-hidden">
                    <summary class="cursor-pointer list-none flex items-center justify-between gap-3 p-5 hover:bg-slate-700/40 transition">
                        <span class="font-bold text-white text-lg">Water Heaters</span>
                        <span class="text-blue-400 text-2xl font-bold flex-shrink-0 transition-transform duration-300 group-open:rotate-45" aria-hidden="true">+</span>
                    </summary>
                    <div class="px-5 pb-5 pt-1 border-t border-slate-700">
                        <ul class="list-disc list-inside space-y-1 pl-1">
                            <li class="text-slate-300 text-sm">Water Heater Installation</li>
                            <li class="text-slate-300 text-sm">Water Heater Repair</li>
                            <li class="text-slate-300 text-sm">Tankless Water Heater Installation</li>
                            <li class="text-slate-300 text-sm">Heat Pump Water Heater Installation</li>
                            <li class="text-slate-300 text-sm">Solar Water Heater Installation</li>
                        </ul>
                    </div>
                </details>
                <details class="group bg-slate-800 rounded-xl border border-slate-700 overflow-hidden">
                    <summary class="cursor-pointer list-none flex items-center justify-between gap-3 p-5 hover:bg-slate-700/40 transition">
                        <span class="font-bold text-white text-lg">Water Treatments</span>
                        <span class="text-blue-400 text-2xl font-bold flex-shrink-0 transition-transform duration-300 group-open:rotate-45" aria-hidden="true">+</span>
                    </summary>
                    <div class="px-5 pb-5 pt-1 border-t border-slate-700">
                        <ul class="list-disc list-inside space-y-1 pl-1">
                            <li class="text-slate-300 text-sm">Water Treatment Services</li>
                        </ul>
                    </div>
                </details>
            </div>
        </section>

        <!-- FULL SERVICE LIST -->
        <section class="bg-slate-800 rounded-xl p-8 border border-slate-700">
            <h2 class="text-xl font-bold text-white mb-4">Full List of Services We Offer</h2>
            <ul class="columns-1 sm:columns-2 gap-x-10 list-disc list-inside space-y-1">
                    <li class="text-slate-300 text-sm">Water Heater Installation</li>
                    <li class="text-slate-300 text-sm">Water Heater Repair</li>
                    <li class="text-slate-300 text-sm">Tankless Water Heater Installation</li>
                    <li class="text-slate-300 text-sm">Heat Pump Water Heater Installation</li>
                    <li class="text-slate-300 text-sm">Solar Water Heater Installation</li>
                    <li class="text-slate-300 text-sm">Water Treatment Services</li>
            </ul>
        </section>

        <!-- TESTIMONIAL -->
        <section class="border-l-4 border-blue-500 pl-6 py-2">
            <blockquote class="text-slate-200 text-lg italic mb-2">
                "Old water heater started leaking on a Sunday. New tankless installed Monday morning — endless hot showers ever since." — Tom B., Wayzata
            </blockquote>
        </section>

                <!-- PHOTO CAROUSEL -->
        {slides.length > 0 && (
          <section aria-label="Project photos">
            <h2 class="text-xl font-semibold text-white mb-6">Our Work</h2>
            <div id="c105-root" class="relative overflow-hidden rounded-2xl bg-white/5 ring-1 ring-white/10 select-none" tabindex="0" aria-roledescription="carousel">
              <div id="c105-track" class="flex transition-transform duration-500 ease-in-out will-change-transform" aria-live="polite">
                {slides.map((s, i) => (
                  <div class="min-w-full" role="group" aria-roledescription="slide" aria-label={`Photo ${i + 1} of ${slides.length}`} aria-hidden={i !== 0}>
                    <img src={s.src} alt={s.title || s.filename} class="w-full object-cover" style="max-height: 30rem; object-position: center;" loading="eager" decoding="async" />
                  </div>
                ))}
              </div>
              <div id="c105-overlay" class="absolute left-0 right-0 bottom-0 px-6 sm:px-8 pt-16 pb-10 bg-gradient-to-t from-black/85 via-black/60 to-transparent text-white pointer-events-none">
                <h3 id="c105-title" class="text-lg sm:text-2xl font-semibold mb-1"></h3>
                <p id="c105-caption" class="text-sm sm:text-base opacity-90 leading-snug"></p>
              </div>
              {slides.length > 1 && (
                <>
                  <button id="c105-prev" aria-label="Previous photo" class="absolute left-3 top-1/2 -translate-y-1/2 w-10 h-10 rounded-full bg-black/50 hover:bg-black/75 text-white text-2xl font-bold flex items-center justify-center transition z-10">&lsaquo;</button>
                  <button id="c105-next" aria-label="Next photo" class="absolute right-3 top-1/2 -translate-y-1/2 w-10 h-10 rounded-full bg-black/50 hover:bg-black/75 text-white text-2xl font-bold flex items-center justify-center transition z-10">&rsaquo;</button>
                  <div id="c105-dots" class="absolute bottom-3 left-1/2 -translate-x-1/2 flex gap-2 flex-wrap justify-center max-w-xs z-10" role="tablist" aria-label="Select photo">
                    {slides.map((_, i) => (
                      <button role="tab" data-idx={String(i)} aria-label={`Photo ${i + 1}`} aria-selected={String(i === 0)} class={['dot w-2 h-2 rounded-full transition-all duration-300 focus-visible:outline focus-visible:outline-2 focus-visible:outline-white', i === 0 ? 'bg-white scale-125' : 'bg-white/40']} />
                    ))}
                  </div>
                </>
              )}
            </div>
            <p id="c105-counter" class="text-center text-sm opacity-40 mt-3">1 / {slides.length}</p>
          </section>
        )}

        <!-- LEAD FORM -->
        <section id="quote" class="bg-slate-800 rounded-xl p-8 sm:p-10 border border-slate-700">
            <h2 class="text-2xl font-bold text-white mb-1">Get Your Free Water Heaters & Treatment Estimate</h2>
            <p class="text-slate-400 mb-6">Free visit. No obligation. Same-week scheduling available.</p>
            <form name="water-heaters-treatment" method="POST" data-netlify="true" class="flex flex-col gap-4">
                <input type="hidden" name="form-name" value="water-heaters-treatment" />
                <div class="grid grid-cols-1 sm:grid-cols-2 gap-4">
                    <input type="text"  name="name"  placeholder="Your Name"     required
                           class="bg-slate-700 border border-slate-600 rounded-lg px-4 py-3 text-white placeholder-slate-400 focus:outline-none focus:border-blue-500" />
                    <input type="tel"   name="phone" placeholder="Phone Number"  required
                           class="bg-slate-700 border border-slate-600 rounded-lg px-4 py-3 text-white placeholder-slate-400 focus:outline-none focus:border-blue-500" />
                </div>
                <input type="email" name="email" placeholder="Email Address" required
                       class="bg-slate-700 border border-slate-600 rounded-lg px-4 py-3 text-white placeholder-slate-400 focus:outline-none focus:border-blue-500" />
                <input type="text" name="address" placeholder="Service Address (City + Zip)"
                       class="bg-slate-700 border border-slate-600 rounded-lg px-4 py-3 text-white placeholder-slate-400 focus:outline-none focus:border-blue-500" />
                <textarea name="description" placeholder="Describe what needs to be done..." rows="3" required
                          class="bg-slate-700 border border-slate-600 rounded-lg px-4 py-3 text-white placeholder-slate-400 focus:outline-none focus:border-blue-500"></textarea>
                <button type="submit"
                        class="bg-blue-600 hover:bg-blue-500 text-white font-bold px-8 py-4 rounded-lg text-lg transition">
                    Get My Free Estimate
                </button>
            </form>
        </section>
    </div>
    <script type="application/json" id="c105-data" set:html={captionsJSON}></script>
</Layout>

<script>
  function initCarousel(prefix) {
    const track = document.getElementById(`${prefix}-track`);
    const root = document.getElementById(`${prefix}-root`);
    const prevBtn = document.getElementById(`${prefix}-prev`);
    const nextBtn = document.getElementById(`${prefix}-next`);
    const counter = document.getElementById(`${prefix}-counter`);
    const titleEl = document.getElementById(`${prefix}-title`);
    const captionEl = document.getElementById(`${prefix}-caption`);
    const dataEl = document.getElementById(`${prefix}-data`);
    const captions = dataEl ? JSON.parse(dataEl.textContent || '[]') : [];
    const dots = document.querySelectorAll(`#${prefix}-dots .dot`);
    const slides = track ? Array.from(track.children) : [];
    if (!track || slides.length === 0) return;
    const total = slides.length;
    let current = 0; let timer;
    function updateCaption(i) {
      const c = captions[i] || { title: '', caption: '' };
      if (titleEl) titleEl.textContent = c.title || '';
      if (captionEl) captionEl.textContent = c.caption || '';
    }
    function goTo(index) {
      current = ((index % total) + total) % total;
      track.style.transform = `translateX(-${current * 100}%)`;
      slides.forEach((s, i) => s.setAttribute('aria-hidden', String(i !== current)));
      dots.forEach((d, i) => {
        const active = i === current;
        d.classList.toggle('bg-white', active);
        d.classList.toggle('scale-125', active);
        d.classList.toggle('bg-white/40', !active);
        d.setAttribute('aria-selected', String(active));
      });
      if (counter) counter.textContent = `${current + 1} / ${total}`;
      updateCaption(current);
    }
    updateCaption(0);
    if (slides.length <= 1) return;
    function startAuto() { clearInterval(timer); timer = setInterval(() => goTo(current + 1), 5000); }
    prevBtn?.addEventListener('click', () => { goTo(current - 1); startAuto(); });
    nextBtn?.addEventListener('click', () => { goTo(current + 1); startAuto(); });
    dots.forEach((d) => d.addEventListener('click', () => { goTo(Number(d.dataset.idx)); startAuto(); }));
    root?.addEventListener('keydown', (e) => {
      if (e.key === 'ArrowLeft') { goTo(current - 1); startAuto(); }
      if (e.key === 'ArrowRight') { goTo(current + 1); startAuto(); }
    });
    root?.addEventListener('mouseenter', () => clearInterval(timer));
    root?.addEventListener('mouseleave', startAuto);
    let touchStartX = 0;
    root?.addEventListener('touchstart', (e) => { touchStartX = e.touches[0].clientX; }, { passive: true });
    root?.addEventListener('touchend', (e) => {
      const diff = touchStartX - e.changedTouches[0].clientX;
      if (Math.abs(diff) > 40) { goTo(diff > 0 ? current + 1 : current - 1); startAuto(); }
    }, { passive: true });
    startAuto();
  }
  initCarousel('c105');
</script>
